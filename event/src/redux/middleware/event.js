import { axiosInstance } from "@/axios/axios";
 
export const eventReducer = ({
  eventname,
  eventprice,
  eventstartdate,
  eventenddate,
  eventdescription,
  eventtype,
  eventlocation,
  availableseat,
  eventcategories,
}) => {
  return async (dispatch) => {
    try{

    }
  };
};
