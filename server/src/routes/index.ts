import { route as eventsRoutes } from "./events";
import { route as usersRoutes } from "./users";
import { route as voucherRoutes } from "./voucher";
import { route as transactionRoutes } from "./location";

export const routes = {
  usersRoutes,
  eventsRoutes,
  voucherRoutes,
  transactionRoutes,
};
