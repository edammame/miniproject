import { route as eventRoutes } from "./events";
import { route as voucherRoutes } from "./voucher";
import { route as locationRoutes } from "./location";
import { route as transactionRoutes } from "./transaction";

export const routes = {
  eventRoutes,
  voucherRoutes,
  locationRoutes,
  transactionRoutes,
};
